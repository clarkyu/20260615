# 你好！作业 / Hi, Homework — PR 全景（可读版）

> **4 天 + 一轮加固 · 227 个 PR**。下面按演进 **阶段 0 → 阶段 14（共 15 段）+ 总结 + 一句话结论 = 17 个部分**，每段可折叠（GitHub 上点 ▸ 展开），只列要点。逐个 PR 的完整详述见 [`docs/PR-DETAILED.md`](PR-DETAILED.md)。

| 阶段 | 主题 | PR 区间 |
|---|---|---|
| 0 | 产品成形 · MVP | #1–22 |
| 1 | 五段式重构（产品骨架） | #23–27 |
| 2 | 质量审查 + 上手 + 默认值 | #28–32 |
| 3 | 题库系统 · 内容资产 | #33–41 |
| 4 | UI 精致度 | #42–43 |
| 5 | 架构加固（校验 + 异步批改 + 分层） | #44–61 |
| 6 | 安全修复 + 代码评审 ①–⑧ | #62–72 |
| 7 | 时区 / 上传 / AI 模型 / BYOK | #73–85 |
| 8 | 题库分级 + 全球化 + 2000 句中文 | #86–123 |
| 9 | 多环节作业 | #124–137 |
| 10 | 体验大潮 | #138–165 |
| 11 | 邀请 / 缺交 / 批量 / 时区收尾 | #166–172 |
| 12 | 结构化 Backlog P1–P4 | #173–188 |
| 13 | 第二波全应用复审 | #189–212 |
| 14 | 加固与洞察（E2E·授权集中化·可观测性·AI 校准·测试补强） | #213–227 |
| 总结 | 可改善空间（已大半收口） | — |
| 结论 | 一句话 | — |

---

<details>
<summary><b>阶段 0 · 产品成形 · MVP（#1–22 · 06-15）</b></summary>

- **核心闭环一天立起**：建校 → Excel 名单导入（按学号 upsert、按班列自动建班）→ 发作业 → 学生前置摄像头录制 → 预签名直传 R2 → 老师批改 → 按班导出（#1）。
- **模型 + AI 骨架**：School/ClassGroup/四角色 User/Assignment/Submission；AI 两段式编排（感知→评分）+ **占位适配器**（流程跑通，接 key 即真）（#1）。
- 多班批量发布、各页筛选、院系/专业规范化为实体（#2–8）。
- **⚠️ D1 第一次踩坑**：多班发布把句子插入包进 `$transaction`，D1 无交互事务拿不到自增 id → 发布失败，改逐条 create（#9）。
- 统一登录（学校+学号/工号+密码）、改名「你好！作业」、切域名 + 强制规范域跳转（#14–18）。
- 提交类型（文本/音频/视频/手写）+ 分类；学生录制流重设计（3-2-1 倒计时/竖屏/闭眼提示）、老师逐个批改流、首页行动台 + 待批看板（#11–13, 19–22）。

</details>

<details>
<summary><b>阶段 1 · 五段式重构 · 产品骨架（#23–27 · 06-15~16）</b></summary>

「世界级 AI 教育产品」五段地基，逐段落地：

1. **评分领域层 + 置信度分流**（#23）：`decideReview` 高置信免复核、未知 fail-safe。
2. **学练闭环**（#24）：正式交前「练一练」无限次、不计正式次数（`PracticeAttempt`）。
3. **AI 先批、只看例外**（#25）：提交即后台自动评分（`waitUntil`）、高置信自动通过、未配 key 优雅退回全人工不阻塞学生。
4. **学情分析**（#26）：零新表实时聚合——提交率/均分/风险识别/最薄弱句。
5. **AI 备课出题**（#27）：给主题或拍课本照片 → 起草整份作业回填表单。

</details>

<details>
<summary><b>阶段 2 · 质量审查 + 上手 + 默认值（#28–32 · 06-16）</b></summary>

- **质量审查修 4 个 CRITICAL**（#28）：媒体管线被 CSP/Permissions-Policy 掐死、练习接口 IDOR、置信度从未接真实模型（「高置信自动通过」生产从未触发）、裸 `JSON.parse(violations)` 致整页 500。
- 老师上手：加同事老师、手动建空班、三步引导（#29）。
- 教学默认值学习导向 + **平时成绩（练一练）vs 测试成绩（提交）分轨**（#30–32）。

</details>

<details>
<summary><b>阶段 3 · 题库系统 · 内容资产（#33–41 · 06-16）</b></summary>

- **三段式跟读**：每句 = 中心句 + 解释句 + 情境例句，各中英对照 + 整套跟读视频（#33–34）。
- 从题库一键发布 → 学生竖屏跟读 → **逐句录音逐句提交** → **逐句 AI 评分**（准确度·0.7 + 完整度·0.3，高分自动通过）（#35, 37, 40）。
- 题库可编辑、学情一键复习薄弱句、学生「我的成长」+ 反馈细节、全班成绩单导出（#36, 38–39, 41）。

</details>

<details>
<summary><b>阶段 4 · UI 精致度（#42–43 · 06-16）</b></summary>

- **暗色模式**真正可用（无闪烁初始化脚本 + 主题切换）、**恢复双指缩放**（WCAG 1.4.4）、中文系统字体调校（零下载）（#42）。
- 骨架屏 + 进步高光（`prefers-reduced-motion` 自动关）、品牌字形「你」、a11y（aria-current/pressed）、**题库进老师底部导航**（#43）。

</details>

<details>
<summary><b>阶段 5 · 架构加固 · 校验 + 异步批改 + 分层（#44–61 · 06-16）★可维护性核心</b></summary>

- **zod 校验边界**：所有 action 输入过 `parseForm`、消息即 i18n key；**登录刻意不 zod 化**（保留恒定时间假校验防时序侧信道）（#44–46）。
- **异步批改工作流**（#47）：`GradingJob` 持久队列 + 乐观锁认领（绕开 D1 无事务）+ 指数退避 + 死信 + 面板自愈重扫。
- **彻底分层 action→domain→repo**（#48–61）：`schoolId ?? -1` 哨兵集中到 repo 一处（越权拦截只此一份）；22 个页面只读查询全收进仓储（`grep prisma. src/app` → 0）；配置集中到 `config.ts`（脱敏自检，绝不打印密钥值）。
- **lint 硬约束**：禁止 action 直接碰 prisma（auth.ts 豁免）；架构归档为 Skill（#53–54）。

</details>

<details>
<summary><b>阶段 6 · 安全修复 + 代码评审 ①–⑧（#62–72 · 06-16）</b></summary>

- **限流改 D1 共享存储**（#62）：原 per-isolate 内存计数 → 有效上限被绕开；改一条原子 `INSERT…ON CONFLICT…RETURNING` 跨 isolate 共享。
- 转班会话自愈、删除表单防双击（#63–64）。
- **代码评审 ①–⑧**（#65–72）：补 `FAILED` 计 attempt（防白得一次提交）、草稿盖已评分（漏分）、录像组件生命周期/内存泄漏、批改状态机走仓储、限流防御纵深、清理死键、补测 submit/shadow/offerings/roster（→113 通过）。

</details>

<details>
<summary><b>阶段 7 · 时区 / 上传 / AI 模型 / BYOK（#73–85 · 06-16）</b></summary>

- **时区错误修复**：`datetime-local` 本地墙钟被 UTC 服务器当 UTC → 学生看「未开放」8 小时；改客户端时区转换 + 历史数据回拨迁移 0018（#73–75）。
- 上传失败显具体原因、R2 桶绑定修复（#76–77）。
- **AI 模型目录 + BYOK**（#80–85）：各家范围/模态/价格目录；老师自带 key **AES-256-GCM 加密**（只存密文+末 4 位、明文永不回传）；批改/备课/练习三类用途用本人 key（没配/解密失败回退平台 key、零回归）；每人自己的默认模型。

</details>

<details>
<summary><b>阶段 8 · 题库分级 + 全球化 + 2000 句中文（#86–123 · 06-16~17）</b></summary>

- **课程脊柱** `taxonomy`（CEFR/技能/领域/L1）+ 句集分级元数据 + 按级别筛选（#86–89, 93）。
- **真·全局共享题库**（#90）：`ChunkSet.schoolId` 可空、整表重建迁移 0022 —— 具体 `schoolId` 永不匹配 `NULL` 行 → 全局官方集对老师天然只读。
- **English Flow 2000 整本接入**（仅服务端不进客户端包）+ 通用题包界面导入（粘贴拆套不改代码）+ **有界可恢复导入**（#94–96）。
- **三语 UI**（#106）：完整西语字典（与 zh/en 1:1 对齐）+ 三态语言循环。
- **2000 句中文回填**（#113–120）：三态显示开关 + 回填管线（稀疏下标译文、巨大数据文件不动、超管「回填」幂等分批 `replaceChunks` 保留 set id）+ 7 个累加 PR 全译完 🎉。
- 学生可属多班级 + 彻底对等化（删 `User.classId`，迁移 0025 整表重建）（#110–111）。

</details>

<details>
<summary><b>阶段 9 · 多环节作业（#124–137 · 06-17~18）★大特性</b></summary>

- 作业从「扁平单一要求」升级为「**有序环节列表**」，每环节各自内容/提交要求/时间窗/次数/是否计分（#124–125）。
- **风险控制三段交付**：老作业自动转单环节（纯增量迁移 0027）、单环节逐字不变、新功能影响面锁在「全新多环节作业」（#126–127）。
- **🔴 #130 致命修复**：编辑作业（哪怕一字没改）→ `phase.deleteMany` + `onDelete:Cascade` → **删光所有学生提交**；修为按 phase id 就地对账（保留→update 不换 id）。
- 并发 fencing（终态写入加 `PROCESSING` 守卫）、统计全线按环节汇总、导出按环节拆列、作业模板（#131–134, 136–137, 177）。

</details>

<details>
<summary><b>阶段 10 · 体验大潮（#138–165 · 06-18）★留存/教学闭环</b></summary>

- **反馈闭环**（#138）：出分预期文案、弱句一键直练、跟读逐句反馈。
- **激励与画像**：积分系统（完全派生不存余额 + 连续打卡🔥）、成绩趋势图、「我的薄弱点」画像、出分站内未读红点（#140–146）。
- **正式测试·强分层**（#147）：环节级强制录像 + 违规记录 + 横幅 → 成绩更真实。
- **学校管理员做成真角色**（#148–154）：激活 `SCHOOL_ADMIN`、建校人即管理员、结构性操作收权、教师端隐藏入口、**角色面板切换器**（视角聚焦不提权）。
- **安全邀请链接**（#163）：只存 token 哈希、一次性、7 天过期（替代「学校码自助加入」的越权风险）。
- 批阅减负、未提交名单、at-risk 学生下钻、自由练习开关（#152–160, 164–165）。

</details>

<details>
<summary><b>阶段 11 · 邀请 / 缺交 / 批量 / 时区收尾（#166–172 · 06-18~19）</b></summary>

- 邀请链接管理（待用列表 + 撤销）（#166）。
- 批阅「标记缺交」（不计 0、纯 Prisma 枚举零迁移）、**批量打分/反馈**（多选）（#167–168）。
- 名单导入失败行明细（行号 + 原因 i18n）、消除日期首屏 UTC→本地闪现、题库发布前内嵌编辑（#170–172）。

</details>

<details>
<summary><b>阶段 12 · 结构化 Backlog P1–P4（#173–188 · 06-19）</b></summary>

把全项目审计的待办整理成预编号 PR 队列（`docs/BACKLOG.md`），逐条清：

- **P1**：评阅失败可见化、导入失败行反馈、补关键单测、多阶段导出拆列、自描述同步（#174–178）。
- **P2**：邮件失败如实告知、慢作业心跳防重领、成绩单省内存、PWA 真实图标（#179–182）。
- **P3**：录制前隐私同意、视频留存定期清理（默认关）、Whisper/Claude **真接 provider**（删光占位 Stub）（#183–186）。
- **P4**：评阅定时排空——**受保护路由 + GitHub Action 的「安全 drain」替代原生 Queues**（无干净注入点、运行期无法本地验证、改错断所有部署）（#187）。

</details>

<details>
<summary><b>阶段 13 · 第二波全应用复审（#189–212 · 06-19）</b></summary>

- 媒体清理补全（跟读/练一练/题库视频）、专业可无院系导入（#191，clark 实测真实 bug，迁移 0037）。
- **校内 IDOR 修复**（#192）：同校老师枚举 id 触达他人数据 → repo 层每处「按 id」staff 读写统一按角色 scope。
- **a11y 三刀**（#194–196）：表单可访问名称、动态状态 aria-live、弹窗焦点/键盘；+ 补全 reduced-motion（#199）。
- 运维 runbook、错误边界（not-found/global-error）、登录极光背景、**按页本地化标题**（静态+动态）、加载骨架、**性能审计（无数据库 N+1）**（#197–207）。
- **测试加固**：评分编排/越权写入回归、权限变更守卫、班级删除孤儿清理；**真 SQLite 集成测试**（37 迁移重放 + 真 Prisma + IDOR/真级联/raw SQL/名单导入端到端）（#193, 200, 208, 211–212）。

</details>

<details>
<summary><b>阶段 14 · 加固与洞察（#213–227 · 06-21）</b></summary>

- **项目记录**：逐个 PR 详评（`docs/PR-DETAILED.md`，#213）+ 可读全景版（本文件，#214）。
- **三层 E2E**（#215–217）：① 登录→提交→批改主流程的领域级集成（真 SQLite harness）；② server action 端到端（内存 cookie jar + iron-session 真往返 + Next shim）；③ **Playwright 真浏览器**（`next dev` 接 miniflare 真 D1/R2，学生/老师/错密码三路）——刻意不进 CI（环境敏感、慢）。
- **授权集中化**（#218）：把三仓字节相同的 staff 归属过滤（#192 的成因）抽成 `lib/repo/scope.ts` 的 `offeringScopeFor` **单一来源**，真 SQL 集成测试守（#227）。
- **可观测性**（#219–220）：结构化日志 `lib/log.ts`（一行 JSON，堆栈不再丢——Workers 上 `JSON.stringify(error)==="{}"`）；迁移 ~25 处服务端 console；**lint 护栏** `no-console` 锁死不回退。
- **AI 批改校准**（#221/222/224/226）：把每条提交早存的 `aiScore` 与 `teacherScore` 变信号——老师改过分的提交里 AI 偏严/偏松/一致；教师·单课头 → 管理员·全校 → **按老师拆分**（揪异常老师）→ 加 180 天时间窗（性能 + 跟当前模型）。
- **测试补强**（#223/225/227）：i18n 三语平价（key 集 + 插值标记，自动化手动 grep）、crypto AES-GCM 往返/篡改、upload-error 判别、模板解析 fail-closed、校准查询租户边界真 SQL。**测试 285 → 337**。

</details>

<details>
<summary><b>📌 总结 · 可改善空间（#213–227 后：六项已收口四项）</b></summary>

> #210/#212 复盘点名的六项里，**四项已收口**（✅）；剩余开放项多需仓库设置 / 外部服务 / 线上数据，非纯代码（🟡）。

1. ✅ **测试深度** —— 集成（#211/212/227）+ **三层 E2E**（#215–217）已补；pure 模块也补齐（#223/225）。浏览器 E2E 刻意不进 CI 是有意取舍。
2. ✅ **授权集中化** —— `offeringScopeFor` 单一来源（#218），三仓复用 + 真 SQL 集成测试守（#227）。
3. 🟡 **运行时可观测性** —— 结构化日志 + lint 护栏已做（#219/220）；**外部错误聚合（Sentry 类）仍未接**（需第三方/部署密钥）。
4. 🟡 **CI 门禁** —— 加了 no-console 护栏；**SAST / Dependabot / bundle 预算 / axe a11y 仍未做**（部分需仓库设置或外部工具）。
5. 🟡 **AI 层校准** —— 老师对 AI 的修正已变成**可观测校准信号**（#221–226）；**模型 id 写死、阈值/权重经验值**仍是固有项，需线上数据闭环。
6. ✅ **时区** —— 早已统一，本轮复核确认是正确实现（tzo cookie 驱动本地日窗 + 服务端/浏览器一致格式化），非 bug。

**刻意取舍（非缺陷，均已记 tradeoff）：** 原生 Queues/DO 不做（可验证性优先）、定时任务走 Actions cron（分钟级漂移）、取消邮箱验证（易用优先）、防作弊仅前端提示。
**用户行动项：** #191 部署后需重新导入一次 Excel，给现有班级回填「专业」。

</details>

<details open>
<summary><b>🏁 一句话结论</b></summary>

4 天 + 一轮加固、**227 个 PR**，从 0 到一个**功能完整、架构分层干净（action→domain→repo）、安全 / 可访问性 / 运维 / 性能 / 可观测性 / 测试都收口**的多租户教育应用。曾经最大的短板「缺 E2E 与运行时可观测性」**已在 #215–220 补齐**；校准洞察（#221–227）更把「AI 评分可不可信」变成老师与管理员可见的信号。剩余主要是外部服务接入与 CI 门禁——多为仓库设置层面，而非代码缺口。

</details>
